#include <stdio.h>
#include <math.h>

int main()
{
    int a, b;
    printf("Enter two number to get the LCM : ");
    scanf("%d %d", &a, &b);

    printf("The LCM of given numbers %d and %d is %d.", a, b, lcm);
    return 0;
}